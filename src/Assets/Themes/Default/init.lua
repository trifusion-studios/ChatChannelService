local Channels = require(script:WaitForChild("Channels"))
local Chat = require(script:WaitForChild("Chat"))
local Messages = require(script:WaitForChild("Messages"))

return {
	Channels = Channels,
	Chat = Chat,
	Messages = Messages,
}
